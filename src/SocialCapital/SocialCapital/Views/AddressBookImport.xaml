<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 xmlns:i18n="clr-namespace:SocialCapital.Common;assembly=SocialCapital"
			 x:Class = "SocialCapital.Views.AddressBookImport"
			 Title = "{i18n:Translate AddressBookMenuItemTitle}">
	<ContentPage.Content>
		<Label Text = "{Binding Status}" />

		<StackLayout Orientation="Horizontal">
			<Button Text="{i18n:Translate SynchAddressBook}"
					Command="{Binding StartImport}" />
			<ActivityIndicator IsRunning="{Binding IsImportRunning}" />
		</StackLayout>

		<ListView ItemsSource="{Binding ContactsGroups}" IsGroupingEnabled="true" >
			<ListView.GroupHeaderTemplate>
				<DataTemplate>
					<TextCell Text="{Binding GroupName}" />
				</DataTemplate>
			</ListView.GroupHeaderTemplate>
			<ListView.ItemTemplate>
				<DataTemplate>
					<TextCell Text="ContactName" Detail="ContactStatus" />
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</ContentPage.Content>
</ContentPage>
